services:
  - type: web
    name: balik-wika
    env: python
    plan: free
    region: singapore   # your region
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
